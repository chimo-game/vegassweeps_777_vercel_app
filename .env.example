# Required Environment Variables for Vercel

# 1. Firebase Service Account (JSON)
# Go to Firebase Console -> Project Settings -> Service Accounts -> Generate New Private Key
# Paste the entire JSON content here (single line preferred for Vercel env vars)
FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}

# 2. API Secret (Postback Security)
# Generate a random string (e.g., uuid) and set it here.
# You must also add this secret to your Ad Network's postback URL: ...&secret=YOUR_SECRET
API_SECRET=change_me_to_a_secure_random_string
